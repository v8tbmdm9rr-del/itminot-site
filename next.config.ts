import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    // Все изображения — уже оптимизированный локальный webp,
    // поэтому дополнительная обработка через оптимизатор Next.js не нужна.
    unoptimized: true,
  },
  // Разрешает открывать dev-сервер с телефона или другого устройства в локальной
  // сети (например, http://172.20.10.13:3100) — без этого Next.js блокирует
  // подключение hot-reload и страница может зависать на заставке.
  // При необходимости добавьте сюда точный IP из вывода `npm run dev` (строка "Network:").
  allowedDevOrigins: ["172.20.10.*", "192.168.*.*", "10.*.*.*"],
};

export default nextConfig;
